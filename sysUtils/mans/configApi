
Warning

ConfigAPI is made by ben657. If you are creating software using this api, leave credit (also you can create "credits" menu).

Functions:

configApi.load(string directory, string file): loads file to RAM. If not exist, creates it.

configApi.save(): writes changes to the file.

configApi.writeVal(string varname, string value): writes variable to ram. After savilg it will look like "varname = value".

configApi.readVal(string varname): returns value of "varname" variable